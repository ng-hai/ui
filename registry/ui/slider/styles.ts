import { tv } from "@/registry/lib/tv.config";

export const sliderStyles = tv({
  slots: {
    root: [""],
    label: [""],
    value: [""],
    control: [""],
    track: [""],
    thumb: [""],
    indicator: [""],
  },
});
