import { tv } from "@/registry/lib/tv.config";

export const tooltipStyles = tv({
  slots: {
    root: [""],
    trigger: [""],
    portal: [""],
    positioner: [""],
    popup: [""],
    arrow: [""],
  },
});
