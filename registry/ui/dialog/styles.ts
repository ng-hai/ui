import { tv } from "@/registry/lib/tv.config";

export const dialogStyles = tv({
  slots: {
    root: [""],
    trigger: [""],
    portal: [""],
    backdrop: [""],
    popup: [""],
    viewport: [""],
    title: [""],
    description: [""],
    close: [""],
  },
});
