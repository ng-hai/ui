import { tv } from "@/registry/lib/tv.config";

export const scrollAreaStyles = tv({
  slots: {
    root: [""],
    viewport: [""],
    scrollbar: [""],
    content: [""],
    thumb: [""],
    corner: [""],
  },
});
