import { tv } from "@/registry/lib/tv.config";

export const fieldStyles = tv({
  slots: {
    root: [""],
    label: [""],
    error: [""],
    description: [""],
    control: [""],
    item: [""],
  },
});
